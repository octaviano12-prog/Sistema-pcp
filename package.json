{
  "name": "sistema-pcp",
  "version": "1.0.0",
  "description": "Sistema PCP com frontend React/Vite e backend Express.",
  "private": true,
  "type": "module",
  "main": "server.js",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm --workspace backend run dev\" \"npm --workspace frontend run dev\"",
    "build": "npm --workspace frontend run build",
    "start": "node server.js",
    "seed": "npm --workspace backend run seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=22"
  }
}
